export {};

type ObjectTpe = {
   name:string;
    age:number;
   };

   interface ObjectInterface {
    name:string;
    age:number;
   }

let object: ObjectInterface = {
  name:'Daiki-man',
  age:23
};